import { Snake2SentencePipe } from './snake2-sentence.pipe';

describe('Snake2SentencePipe', () => {
  it('create an instance', () => {
    const pipe = new Snake2SentencePipe();
    expect(pipe).toBeTruthy();
  });
});
